function trueOrFalse(wasThatTrue) {
  // Only change code below this line
if(wasThatTrue){
  return "Yes, that was true";              // The code here means that if(true) return this
}                                      
return "No, that was false";                // else this . We don't need to use else in such cases because at the end of the function we have to 
}
  trueOrFalse(true);
  trueOrFalse(false);

  // Only change code above this line

